specVersion: "v1beta"
serviceId: "bemestar"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bemestar-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
